import { CreatePastelModel } from './create-pastel.model';

describe('CreatePastelModel', () => {
  it('should create an instance', () => {
    expect(new CreatePastelModel()).toBeTruthy();
  });
});
