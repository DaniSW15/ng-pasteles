import { CreateClienteModel } from './create-cliente.model';

describe('CreateClienteModel', () => {
  it('should create an instance', () => {
    expect(new CreateClienteModel()).toBeTruthy();
  });
});
